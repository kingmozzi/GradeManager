<UserControl x:Class="GradeManager.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding StudentsVM}">
    <DockPanel Margin="8">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 8">
            <Button Content="학생 추가" Command="{Binding AddCmd}"/>
            <Button Content="학생 삭제" Command="{Binding RemoveCmd}" Margin="8 0 0 0"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Students}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="이름"  Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="번호"  Binding="{Binding Number, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="학년"  Binding="{Binding Grade, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="반"    Binding="{Binding Class, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
